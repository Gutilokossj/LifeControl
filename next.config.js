/** @type {import('next').NextConfig} */
const nextConfig = {
    devIndicators: {
      autoPrerender: false,
    },
    serverRuntimeConfig: {},
    publicRuntimeConfig: {},
  };

module.exports = nextConfig;
